name: Checkout code
on: push
jobs: 
  checkout-job:
     runs-on: ubuntu-latest
     steps:
       - name: Checkout repo
         uses: actions/checkout@v4
       - name: print all files 
         run: ls -la
